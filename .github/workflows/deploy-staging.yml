# Deploy to Staging — implemented in Story 1.2
name: Deploy Staging

on:
  push:
    branches: [main]

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Staging deploy implemented in Story 1.2"
