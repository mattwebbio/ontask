{
  "name": "@ontask/mcp",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings",
    "test": "vitest run"
  },
  "dependencies": {
    "hono": "^4.12.9"
  },
  "devDependencies": {
    "vitest": "^3.2.4",
    "wrangler": "^4.4.0"
  }
}
