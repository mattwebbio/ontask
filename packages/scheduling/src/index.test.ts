// Smoke test — verifies the scheduling module loads without errors.
// Real tests are added in Epic 3 when the scheduling engine is implemented.
import { describe, it } from 'vitest'
import {} from './index.js'

describe('@ontask/scheduling', () => {
  it('module loads without errors', () => {
    // Intentionally empty — the import above is the test
  })
})
